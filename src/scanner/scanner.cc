#include "scanner.hh"

namespace scanner
{
    Scanner::Scanner(const std::string& in)
    {
        size_t len = in.size();
        for (size_t i = 0; i < len; ++i)
        {

        }
    }

    void Scanner::Scan()
    {
        /// TODO
    }
}